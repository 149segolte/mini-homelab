apiVersion: v1
kind: Service
metadata:
  name: copyparty
  namespace: copyparty
spec:
  selector:
    app: copyparty
  ports:
    - name: http
      port: 3923
      targetPort: http
    - name: sftp
      port: 3922
      targetPort: sftp
