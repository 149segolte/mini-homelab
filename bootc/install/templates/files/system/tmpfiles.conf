# /etc/localtime is a symlink, and overlay.py only writes regular files, so it
# is created here instead. `+` replaces whatever the image shipped.
L+ /etc/localtime - - - - ../usr/share/zoneinfo/${TIMEZONE}
