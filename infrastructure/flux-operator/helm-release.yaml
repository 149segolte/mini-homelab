apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-operator
  namespace: flux-system
spec:
  interval: 30m
  releaseName: flux-operator
  chart:
    spec:
      chart: flux-operator
      version: "0.59.*"
      sourceRef:
        kind: HelmRepository
        name: flux-operator
        namespace: flux-system
