apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - external-secret.yaml
  - deployment.yaml

configMapGenerator:
  - name: cloudflared
    namespace: cloudflared
    files:
      - config.yaml
