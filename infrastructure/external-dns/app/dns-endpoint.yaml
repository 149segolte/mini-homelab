apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: primary-dns-endpoint
  namespace: external-dns
spec:
  endpoints:
    - dnsName: node.${DOMAIN}
      recordTTL: 300
      recordType: A
      targets:
        - 172.19.149.1
